[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "teams-mattermost-migration-workflow-engine"
version = "0.1.0"
description = "Flowable Enterprise Workflow Engine Microservice"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "tmmp-integrations-shared",
    "tmmp-integrations-flowable",
    "tmmp-integrations-mattermost",
    "tmmp-integrations-clickhouse",
    "fastapi>=0.111.0",
    "uvicorn>=0.30.0",
    "pydantic>=2.7.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
tmmp_workflow_engine = ["py.typed"]
