[allowlist]
description = "Global allowlist"
paths = [
    "pyproject.toml",
    "scratch/.*",
]

[[rules]]
description = "Generic API Key"
regex = '''(?i)(api_key|apikey|secret_key|auth_token)\s*=\s*['"][0-9a-zA-Z_-]{16,}['"]'''
tags = ["key", "secret"]
